#include<iostream>
using namespace std;
int main(){
    int n;
    cout << "enter the number of processes";
    cin >> n;
    int at[n], bt[n], wt[n], tat[n], ct[n];
    bool comp[n];

    for (int i = 0; i < n;i++){
        cout << "Enter the arrival and Burst of P" << i + 1<<":";
        cin >> at[i] >> bt[i];
        comp[i] == false;
    }

    int time = 0;
    int done = 0;

    while(done<n){
        int minbt = 9999;
        int index = -1;
     }

     for (int i = 0; i < n;i++){
        if
     }